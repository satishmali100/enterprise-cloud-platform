apiVersion: v1
kind: Service

metadata:
  name: enterprise-webapp-service

spec:
  type: NodePort

  selector:
    app: enterprise-webapp

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080